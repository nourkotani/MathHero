# The Tripo sources (ADR 0011) are large: git LFS. Never the game models in
# src/renderer/models: Vite would inline an LFS pointer in their place.
scripts/blender/sources/tripo/**/*.glb filter=lfs diff=lfs merge=lfs -text
