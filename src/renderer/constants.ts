// Arena layout and shared action timings, used across the renderer modules.

/** Where the fighters stand, in world x. */
export const HERO_X = -2.4;
export const DUMMY_X = 2.4;

export const ATTACK_DURATION = 0.55;
export const STAGGER_DURATION = 0.6;
/** The coiled wind-up before a strike explodes out — weight, not lag. */
export const ANTICIPATION_DURATION = 0.12;
