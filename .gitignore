node_modules/
dist/
test-results/
playwright-report/
.playwright-mcp/

# Blender output (ADR 0007): only the compressed .glb in src/ is committed.
build/
*.blend
*.blend1

bash.exe.stackdump
__pycache__/

# Tripo CLI state (the last task id); the records live in models.json.
.tripo/

# Agent worktrees (Claude Code): each is its own checkout.
.claude/worktrees/
